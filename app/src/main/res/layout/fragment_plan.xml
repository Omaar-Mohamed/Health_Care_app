<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/fragment_week_plan.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">



    <!-- Meal Type Spinner (Dropdown Menu) -->
    <Spinner
        android:id="@+id/mealTypeSpinner1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
android:layout_marginTop="50dp"
        android:layout_centerHorizontal="true" />

    <!-- RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/weekPlanRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/mealTypeSpinner1"
        android:padding="16dp"
        android:clipToPadding="false" />

    <!-- Empty View -->
    <TextView
        android:id="@+id/emptyView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="No plans for the week yet"
        android:visibility="gone" />

</RelativeLayout>
